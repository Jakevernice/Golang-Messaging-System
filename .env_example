# Example Database Config
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_NAME=messaging_app
DB_SSLMODE=disable

# JWT Secret Key
JWT_SECRET=supersecretkey

# Server Port
PORT=8080

# Token durations (can be in seconds as integer or valid Go duration format like "15m", "24h")
ACCESS_TOKEN_DURATION=15m    # 15 minutes
REFRESH_TOKEN_DURATION=168h  # 7 days

